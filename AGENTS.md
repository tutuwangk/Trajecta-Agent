# AGENTS.md

## 项目定位

这是本地可跑的旅行路线整理 MVP。前端让用户输入旅行需求和旅行资料，后端使用真实 LLM 和高德 Web 服务识别地点、自动整理地点池、计算路线并生成可调整的每日行程。

## 技术栈

- 前端：`frontend/`，Next.js App Router，TypeScript，Tailwind CSS。
- 后端：`backend/`，FastAPI，Pydantic，SQLite。
- 验证：后端用 `pytest`，前端用 `pnpm run typecheck` 和 `pnpm run build`。

## 关键规则

- 不读取或提交 `.env`；使用 `.env.example` 说明环境变量。
- 第一版不使用 mock 路线。缺少真实 `LLM_API_KEY` 或 `AMAP_API_KEY` 时，应返回可读错误。
- LLM 请求默认信任系统代理环境；需要绕过代理时使用 `LLM_TRUST_ENV=false`。
- 如需区分规划与文案模型，可选配置 `LLM_PLANNING_MODEL` 和 `LLM_COPY_MODEL`；未设置时都回退到 `LLM_MODEL`。
- 前端跨电脑或线上部署时必须设置 `BACKEND_API_BASE_URL`；本地开发未设置时默认代理到 `http://127.0.0.1:8000`。
- 只有地图地点确认成功，且最终决策为 `include` 或 `optional` 的地点，才能进入正式路线。
- 用户不操作地点池时，后端应使用系统默认地点决策继续规划。
- 手动改地点名称后，必须重新做地点确认。
- 连锁品牌点击“顺路安排”时，只记录路线意愿，不提前锁定具体门店；具体门店在规划阶段结合整条路线决定。
- 只要地点池事实发生变化，例如重新识别、改名重搜或修改地点决策，旧的 `itinerary` 和 `revision_history` 都应失效，前端必须提示用户重新生成路线。
- 行程强度按“从酒店出发到晚上回到酒店”的全部外出时间计算，不按地点数量计算。
- 强度只分为 `轻松` 和 `特种兵`：每日总外出时间 7 小时以内显示为轻松，超过 7 小时显示为特种兵；用户选择轻松时仍应优先保留必去地点，待定地点可在强度较大时放弃。
- 外出总时长包括酒店往返交通、点间交通、用餐、排队、游玩和休息。
- 首页表单应结构化提交 `user_profile`；酒店名必须保存到 `hotel_name`，不要只依赖自然语言解析。
- LLM 在选点、分天、排序阶段只输出路线骨架；时间线、交通边、饭点、到达时间和每日总外出时长必须由系统确定性生成。
- 每日路线的时间线由前端代码根据后端规范化后的行程 JSON 绘制，不让 LLM 生成可视化图形；前端不再自行叠加隐藏交通余量，也不能用前一站推算去覆盖后端给出的 `arrival_time` 或 `meal_breaks` 时间。
- 午餐和晚餐优先匹配已纳入当天路线的真实餐饮点；如果没有合适餐饮点，只显示“午餐：就近用餐”或“晚餐：就近用餐”，不要把博物馆、景区等非餐饮地点写成用餐标题。
- 前端可见文案面向普通旅行用户，避免展示 `POI`、匹配度、后端端口、会话、Agent 等开发者词汇。
- 首页保持左侧“行程设置”约 3/5、右侧“资料与地点”约 2/5；“识别地点”放在资料模块底部，“生成路线”放在识别出的地点模块底部。

## 常用命令

```bash
backend/.venv/bin/python -m pytest
cd frontend && pnpm run typecheck
cd frontend && pnpm run build
```

如果 `pnpm run build` 在受限环境里因创建进程或绑定端口失败，需在非受限本机环境复测；当前本机环境可完成生产构建。

## 文档入口

- `README.md`：安装、启动、使用流程和验证。
- `docs/ARCHITECTURE.md`：系统结构、数据流、约束和当前边界。
- `docs/HANDOFF.md`：当前交接状态、验证结果和后续注意事项。
